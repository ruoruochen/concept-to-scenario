---
name: concept-to-scenario
description: 把阅读中的概念转化成限时真实场景训练，分析用户的第一反应，不讨好地评价反应质量，生成“如果……就……”行为触发器，并用中文简洁保存触发器和暂存想法。适用于用户希望阅读真实改变行为、知道概念但做不到、想做场景模拟/行为排练/触发器创建，或担心信息过载时。
---

# 概念到场景

这个 skill 用来把读到的概念转化成行为练习。目标不是解释得更多，而是在真实处境里产生一个小而可重复的改变。

## 核心原则

保持闭环很小。如果用户开始追更多概念、更多链接、更多计划，要把用户拉回“一个场景 + 一个反应”。

默认流程：

1. 确认概念和来源书籍。
2. 先让用户选择场景类型，再生成场景。
3. 给出一个具体场景，并限定作答时间。
4. 分析用户的第一反应，不讨好。
5. 给出更成熟的反应，并说明如何想到这个反应。
6. 生成一句“如果……就……”触发器。
7. 询问是否保存触发器；暂时不做的想法放入 stash。

## 场景类型选择

除非用户明确要求代选，否则不要在用户选择类型前直接生成场景。

只提供这五类：

- 日常生活
- 工作
- 友情
- 爱情
- 亲情

如果用户已经信息过载，替用户推荐一个选项，并用一句话说明理由。

## 限时反应

用户作答前，必须给出明确时间限制。

默认：60 秒。

想捕捉原始本能时，用 30 秒。亲情、爱情等情绪复杂场景最多用 90 秒。

提示格式：

```text
请在 60 秒内回答：你会怎么说/怎么做？不要写完美答案，写第一反应。
```

用户没有回答前，不继续分析。如果用户给的是明显修饰过的完美答案，要追问“第一反应是什么”。

## 反应分析

直接、公正、有用。不要默认安慰用户。

分析时包含：

- 做得好的地方
- 这个反应在保护什么
- 可能启动了哪种旧模式
- 哪里偏离了这个概念
- 在现实里会带来什么风险
- 概念应用评分：1-5 分，并给一句证据

不要病理化用户。推测动机时使用“可能”。

## 更成熟的反应

给出更好的反应，但必须说明思考路径。

包含：

- 更成熟的说法/做法
- 当下要识别的内在信号
- 边界：哪些归我负责，哪些不归我负责
- 代价：我需要忍受哪种不舒服

更成熟的反应要真实可做，不要像圣人答案。

## 触发器生成

每个场景默认只生成一个触发器，除非用户要求更多。

格式：

```text
如果……就……
```

好的触发器应该：

- 有具体触发信号，不是抽象情绪
- 只有一个动作，不是一套哲学
- 压力下也能记住
- 和概念、场景类型有关

例子：

```text
如果我开始急着解释来换取认可，就先停 3 秒，提醒自己：对方的评价不是我的任务。
```

## 保存记录

用户要保存触发器时，使用 `references/trigger-record-template.md`。

推荐个人文件：

```text
~/Documents/又又触发器.md
```

如果写入这个位置需要权限，就请求权限。如果用户想放在当前项目里，则保存到：

```text
./又又触发器.md
```

保存内容必须中文、简洁，不保存完整聊天记录。

## Stash 暂存库

当用户提到“以后想做，但现在先别做”的功能或想法时，放入 stash，避免占用注意力。

创建或更新 stash 前，阅读 `references/stash.md`。

推荐个人文件：

```text
~/Documents/又又stash.md
```

每条 stash 只保留：标题、分类、保留原因、状态。全部用中文。

## 第一版先不做的内容

如果出现这些想法，除非用户明确要求现在实现，否则放入 stash：

- 24 小时后复测
- 每日推送上限
- 触发器队列平衡
- 多次场景测试记录
- 真实场景触发日志
- 多次尝试后的趋势分析
- 价值观地图可视化

## 输出风格

用短小分段。除非用户正在做设计，否则避免长列表。

当用户信息过载时，明确说出“这次先忽略什么”。
